<%= form_with model: @budget, url: budget_path(@budget.uid), method: :patch do |form| %>
  <%= form.label :amount %>
  <%= form.number_field :amount, min: 1, step: 0.01, placeholder: "100.00" %>
  <%= form.submit "Save" %>
<% end %>
