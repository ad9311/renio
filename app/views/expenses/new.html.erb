<%= render "shared/breadcrumbs", dataset: breadcrumbs_for(:expenses, :new) %>
<div class="card">
  <h2 class="title title-bottom-margin">New expense</h2>
  <% if @budget.amount.positive? %>
    <%= render "form" %>
  <% else %>
    <p>Plase add an amount to the budget:
      <%= link_to @budget.date, edit_budget_path(@budget), class: "link" %></p>
  <% end %>
</div>
